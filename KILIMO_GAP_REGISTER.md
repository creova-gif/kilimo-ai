# KILIMO_GAP_REGISTER

Severity: **P0** prevents app from functioning · **P1** major feature incomplete · **P2** important defect · **P3** improvement.
Status: OPEN · IN PROGRESS · FIXED (with test) · EXTERNAL (needs someone else).

| ID | Area | Sev | Description | Evidence | Proposed solution | Owner | Status | Test required |
|---|---|---|---|---|---|---|---|---|
| G-001 | Environment | P1 (was P0) | Data volume ~100% full: 232 MiB free at first check (1.8 GiB after removing this session's downloads). Caused `ENOSPC` during the JS bundle step of the iOS build. | `df -h /System/Volumes/Data`; xcodebuild log `ENOSPC … main.jsbundle`. `~/.cache` = 97 GB, `~/.npm` = 7.8 GB (not created by this work). | User decision: free space from `~/.cache` / npm cache / unused Docker images (16.9 GB reclaimable). I will not delete data I did not create. | User | MITIGATED — cleared npm download cache (`npm cache clean --force`, 4.6 GB, regenerable) + this session's downloads → 6.2 GiB free. `~/.cache` (97 GB) untouched; recommend user review. | Build completes from clean |
| G-002 | Tooling | P0→closed | Simulator touch input via the `control` tool has no effect (taps report success, UI unchanged, on both a system alert and in-app tabs); `screenshot` returns `captureFailed` after a simulator reboot. `simctl` capture still works. | 4 taps on "Open in Expo Go?" alert + 1 on Settings tab, verified with independent `simctl io screenshot`. | Drive the app with Maestro (XCTest-based) for journeys and E2E; use `simctl` for capture/launch. | Claude | FIXED — Maestro 2.10.0 (sha256-verified official release) installed at `~/.maestro`; `.maestro/00_smoke_launch.yaml` passes (T-005). `maestro hierarchy` doubles as accessibility inspect. | Maestro flow passes |
| G-003 | Backend | P0 | Both candidate Supabase projects are `INACTIVE`. Prior audit records restore failing with `PaymentRequiredException` (unpaid invoices). `docs/ARCHITECTURE.md` says prod = `vwestumjbrpwlbsewupz` (`creova`); prior audit says `hsjxaxnenyomtgctungx` (`kilimo_ai`). | Supabase MCP `list_projects`; `FEATURE_COMPLETION_REGISTER.md` §C. | Run a **local Supabase stack** from repo migrations as the development backend. Restoring cloud is a billing action for the user; also decide which project is production. | Claude (local) / User (cloud) | IN PROGRESS | Auth + one data round-trip against local stack |
| G-004 | Frontend | P1 | 35 of 159 live Figma screens have no route in the app (Soko marketplace, Community forum/coop/extension dashboards, Harvest Log, Pest Alert, Diagnosis History, Data Export, Emergency Contacts, several Market dashboards). | `docs/kilimo-v2/01_DISCOVERY/FIGMA_SCREEN_INVENTORY.md` | Build from shared primitives; classify each as Implemented / Deferred with reason. | Claude | OPEN | Per-screen render + nav test |
| G-005 | Build | P1 | Xcode 27 rejects pod resource-bundle targets with deployment target < 15.0 (SDWebImage 9.0, Sentry/react-native-maps 11.0, RNSVG 12.4, AsyncStorage 13.4). | xcodebuild error, 5 targets. | `plugins/withPodsDeploymentTarget.js` raises them to 15.1 in the generated Podfile. | Claude | FIXED (build verification pending) | Clean `prebuild --clean` + build |
| G-006 | Database | P0* | App queries a `tasks` table that no migration creates. (*from prior audit; unverified on this branch.) | `FEATURE_COMPLETION_REGISTER.md` §C row 3. | Add migration; verify against local stack. | Claude | OPEN | Migration applies; RLS test |
| G-007 | IoT | P1 | No ingestion path exists: no sensor tables, no gateway, no API. IoT screens are UI over local state. | `docs/ARCHITECTURE.md`; migrations list. | Define schema + ingestion edge function; label anything else as MOCK. | Claude | OPEN | Reading round-trip; stale/offline states |
| G-008 | Marketplace | P1 | Soko offers / orders / price alerts / seller fulfillment have no backend. Market screen renders `SEED_LISTINGS`. | `docs/ARCHITECTURE.md` launch gap #2. | Add tables + RLS + functions, or mark Deferred. No fabricated data. | Claude | OPEN | Offer→order state-machine tests |
| G-009 | Localization | P1 | Swahili copy is hard-coded inline; no resource layer or language switch. | Source read (`app.json` permission strings, screens). | Add EN/SW resource files and a locale provider; migrate screen copy. | Claude | IN PROGRESS — `lib/i18n` (typed EN/SW, `useT`, `translate`) + parity test (5 tests) landed with shared state/auth/permission/AI strings. Remaining: migrate each screen's inline `isSw ? … : …` copy into resources as screens are migrated; long-string layout check on device. | Locale switch E2E; long-string layout check |
| G-010 | AI | P1 | `openai-proxy` needs `OPENAI_API_KEY` (server secret, not available here); `rag-chat` corpus unseeded and not called by the client. | `docs/ARCHITECTURE.md`; `.env.example`. | Run functions locally; without a key the UI must show the "service unavailable" state truthfully. Never fabricate a diagnosis. | Claude / User (key) | OPEN | AI-failure state test |
| G-011 | Notifications | P2 | Push delivery is a TODO in `process-notifications` (DB insert only). | `docs/ARCHITECTURE.md` gap #1. | Implement Expo push send. | Claude | OPEN | Unit test with fake sender |
| G-012 | Config | P2 | No `.env` present; `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY` are empty at bundle time, so client is created with empty strings. | `lib/supabase.ts`; `.env.example`. | Local `.env` pointing at local Supabase; app must fail loudly if unset. | Claude | OPEN | Config-missing state test |
| G-013 | Deps | P3 | 5 Expo packages behind their SDK-54 patch versions (`expo`, `expo-constants`, `expo-file-system`, `expo-local-authentication`, `jest-expo`). | `expo start` warning. | `npx expo install --fix` after build is green. | Claude | OPEN | tsc + jest + build |
| G-014 | Repo hygiene | P3 | Second clone `~/Kilimoai` holds stale uncommitted auth/onboarding WIP and a 162 KB feature register. | `git status` there. | Owner to decide; not modified. | User | EXTERNAL | — |
| G-015 | Repo hygiene | P3 | `.gitignore` did not exclude generated `ios/` and `android/`. | `.gitignore` | Added. | Claude | FIXED | `git status` clean after prebuild |
| G-016 | Security | P1 | Legacy auth falls back to a **mock** whenever `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY` are unset: OTP `123456` is accepted and a `mock-access-token` session + `mock-user-*` id are created (`hooks/useAgroAuth.ts` ~L141–223; also `app/onboarding.tsx:414`). Fails open — a release build shipped without env would admit anyone. | Source read; `if (token === '123456')`. | Fail closed in release builds (`!__DEV__` + no Supabase → show config error), keep mock only behind an explicit dev flag, label it MOCK. | Claude | FIXED — `lib/authMode.ts` (`real`/`mock`/`unconfigured`); hook throws a bilingual not-configured error; mock only in `__DEV__` with `EXPO_PUBLIC_ENABLE_MOCK_AUTH=1`. 6 unit tests. Simulator verification pending. | Unit test: release + no env → auth rejected (done); Maestro: unconfigured build shows error (pending) |
| G-017 | Accessibility | P3 | Some accessibility labels are English while the screen language is Swahili (e.g. "Use email instead", "Go back", "Phone Number Input" on the SW phone-entry step). | `maestro hierarchy` on onboarding step 1. | Route a11y labels through `lib/i18n` when the screen is migrated. | Claude | OPEN | a11y-label locale test |
| G-018 | UX / mobile | P2 | Onboarding phone/email step: the primary CTA "Endelea" is positioned behind the software keyboard on iPhone 15 Pro Max (button y=790–866, keyboard keys y=631–857). A user typing a number cannot reach Continue without dismissing the keyboard (there is no Return key on the numeric pad; tapping outside the field or swiping down does dismiss it — verified — but nothing tells the user to). An automated tap on the button hits the `0` key. | `maestro hierarchy` bounds; failed run `01_auth_unconfigured` showed `7123456780`. | Use keyboard-avoiding layout (KeyboardAvoidingView / pin CTA above keyboard) in the migrated onboarding screens; add Maestro check that CTA is tappable without `hideKeyboard`. | Claude | OPEN | E2E: tap CTA with keyboard up |
| G-019 | Auth UX / i18n | P2 | Auth errors are shown verbatim from the backend/provider in English (e.g. "Error sending confirmation OTP to provider: Authentication Error") even when the UI is Swahili, and give the farmer no actionable guidance. | Device run: number without a test OTP → alert "Hitilafu / Error sending confirmation OTP to provider…". | Map known error codes (invalid/expired code, rate limit, provider down, network) to `auth.*` i18n keys (keys already added: invalidCode, rateLimited, network). | Claude | OPEN | Unit test for error mapper; E2E wrong-code path |
| G-020 | Data quality | P3 | Farm-size field on the profile step is pre-filled with "2" acres, so a farmer can submit an invented size without typing anything; the value feeds AI recommendations. | Device hierarchy on onboarding step 4 (field text `2`). | Start empty with a placeholder; require an entered value. | Claude | OPEN | E2E: Continue disabled until size entered |
| G-021 | Auth / data integrity | P1 | Onboarding reported success ("Agro ID yako imesajiliwa") while **nothing was saved server-side**: five modules each created their own Supabase client, only `useAgroAuth`'s held the OTP session, so `mint-agro-id`, the farmer-profile write and verification submission went out as anon and failed; onboarding silently fell back to a local provisional Agro-ID. | Device E2E run (`02_onboarding_farmer_journey`) reached step 6/6; DB query showed `auth.users` row but 0 rows in `agro_profiles`, `farmer_profiles`, `verification_requests`. | One shared client in `lib/supabase.ts` with Keychain-backed chunked session storage + AppState auto-refresh; all 5 sites use it. Unit tests for the storage adapter. Still to do: stop showing "registered" when `serverMinted:false`. | Claude | IN PROGRESS — shared client landed (13 suites / 182 tests); device re-verification pending | E2E + DB assertion that rows exist after onboarding |
| G-022 | Fabricated data | P1 | The Zustand store seeds EVERY new install with invented content: 3 notifications ("Block B: Moisture dropped to 38%", "Maize price up 12% in Mbeya", "Maize Streak Virus detected in Plot 3"), 2 pre-registered fake national IDs, `farmVitals` (soil health 84, moisture 42, pH 6.8, yield 1.2), `seededDocuments` ("TARI Maize Guide v2.pdf"), `aiAccuracy: 95.8`. `useTasks` also seeded 3 tasks and kept them when the server returned zero rows. The legacy dashboard rendered hard-coded crop progress ("82%", "22 days to harvest"). | `store/useKilimoStore.ts` initial state; device dashboard screenshot; `hooks/useTasks.ts`. | Removed: seeded tasks (hook tests), notifications, registeredIds (+ persist v3 migration purges them from existing installs), legacy dashboard rebuilt on real data. **Still to remove:** `farmVitals`, `seededDocuments`, `aiAccuracy`, and other seeds in market/finance/etc. screens as each is migrated. | Claude | IN PROGRESS | Fresh-install E2E asserts no notifications/tasks/vitals are shown |
| G-023 | Privacy | P2 | The ID-verification prompt on the legacy dashboard displayed the farmer's full 20-digit NIDA in plain text. | Device screenshot (`02_dashboard`). | Mask (show last 4 digits) anywhere a national ID is displayed; new dashboard no longer shows it. Audit remaining screens (profile, verification pending, agro-id). | Claude | IN PROGRESS | Component test: masked output |
