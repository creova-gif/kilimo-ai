import type { enMoney } from './money.en';

/** Swahili strings: finance, mobile-money, upgrade, insurance, wallet-admin. Must define exactly the keys in money.en.ts. */
export const swMoney: Record<keyof typeof enMoney, string> = {
};
