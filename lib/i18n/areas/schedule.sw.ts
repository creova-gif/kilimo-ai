import type { enSchedule } from './schedule.en';

/** Swahili strings: calendar, tasks, forecast, residual Home/Soko/onboarding copy. Must define exactly the keys in schedule.en.ts. */
export const swSchedule: Record<keyof typeof enSchedule, string> = {
};
