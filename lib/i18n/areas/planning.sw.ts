import type { enPlanning } from './planning.en';

/** Swahili strings: forecast, calendar, tasks, crop-planning, soil-analysis, vra-setup. Must define exactly the keys in planning.en.ts. */
export const swPlanning: Record<keyof typeof enPlanning, string> = {
};
