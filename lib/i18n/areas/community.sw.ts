import type { enCommunity } from './community.en';

/** Swahili strings: peer-groups and consultations. Must define exactly the keys in community.en.ts. */
export const swCommunity: Record<keyof typeof enCommunity, string> = {
};
