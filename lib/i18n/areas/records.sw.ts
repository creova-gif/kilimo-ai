import type { enRecords } from './records.en';

/** Swahili strings: livestock, inventory, peer-groups, consultations. Must define exactly the keys in records.en.ts. */
export const swRecords: Record<keyof typeof enRecords, string> = {
};
