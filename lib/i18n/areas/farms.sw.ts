import type { enFarms } from './farms.en';

/** Swahili strings: farms, fields, plots, farm-twin, maps. Must define exactly the keys in farms.en.ts. */
export const swFarms: Record<keyof typeof enFarms, string> = {
};
